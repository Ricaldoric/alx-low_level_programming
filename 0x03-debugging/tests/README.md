# Testing `largest_number`

Compile the test using:

```sh
gcc -Wall -Wextra -Werror -pedantic ../2-largest_number.c test_largest_number.c -o test_largest_number
```

Then run the executable:

```sh
./test_largest_number
```
