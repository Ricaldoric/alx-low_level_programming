#include <stdio.h>
/**
 * main- entry of the program
 *
 * Description: this program prints using 'puts rather than 'printf'
 *
 * Return:  always (0) indicating success
 */

int main(void)
{
	char message[] = "Programming is like building a multilingual puzzle";

	puts(message);

	return (0);
}
